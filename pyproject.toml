[project]
name = "edge-hub"
version = "0.1.0"
description = "EDGE HUB — daily MLB + ATP/WTA betting terminal (BARREL EDGE / ACE EDGE)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
